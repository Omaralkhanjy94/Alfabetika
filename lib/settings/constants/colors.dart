import 'package:flutter/material.dart';
class AppColors {
  static const Color primaryColor = Color(0xffffffaa);
  static const Color secondColor = Color(0xff0055aa);
  static const Color textColor = Color(0xff636ab8);

  static const MaterialColor mainColors = MaterialColor(1, {
      1: primaryColor,
      2: secondColor
  });

}